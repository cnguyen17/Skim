// src/routes/Booking.tsx — Phase 1 placeholder.
// Later (§9): Cal.com embed (DJ set / meeting / equipment) + Web3Forms contact.
import { Placeholder } from "../components/Placeholder";

export default function Booking() {
  return (
    <Placeholder eyebrow="Get in contact" title="Booking">
      Book a set, set up a meeting, or request gear — Cal.com + contact form coming in a later phase.
    </Placeholder>
  );
}
