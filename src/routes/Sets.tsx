// src/routes/Sets.tsx — Phase 1 placeholder.
// Later (§6): DJ Sets (lite-youtube) · Producing · Collaborations, + gallery.
import { Placeholder } from "../components/Placeholder";

export default function Sets() {
  return (
    <Placeholder eyebrow="Watch &amp; listen" title="DJ Sets">
      Live sets, production credits, and collaborations — coming in a later phase.
    </Placeholder>
  );
}
